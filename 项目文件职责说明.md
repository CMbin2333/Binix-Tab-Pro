# BinixOvO 正式版 4.3.0 项目文件职责说明

> **项目名称**：BinixOvO Tab Pro（Chrome 扩展新标签页）
> **版本**：4.1.1（manifest 声明版本）
> **类型**：Manifest V3 Chrome 浏览器扩展
> **核心功能**：响应式动态主题新标签页，提供磁贴式快捷导航、书签/历史/下载搜索管理、多种交互式动态背景（地球/太阳/土星/量子/赛博/流形）、搜索引擎切换、天气/音乐/计时器/翻译等内置小组件
> **生成日期**：2026-06-17

---

## 一、文件总览表

| 文件名 | 大小 | 类型 | 职责说明 |
|---|---|---|---|
| `manifest.json` | 1.2 KB | 配置 | Chrome 扩展清单文件。声明扩展名 "BinixOvO Tab Pro"、版本 4.3.0、Manifest V3 协议，定义 `chrome_url_overrides.newtab` 指向 `index.html`（替换新标签页），注册 `background.js` 为 Service Worker，配置 `popup.html` 为工具栏弹窗，声明 8 项权限（storage/scripting/bookmarks/history/downloads 等）和 4 项主机权限（Bing API / wsrv / allorigins / wttr.in），注册 Ctrl+B 快捷键打开 popup 搜索窗口。 |
| `background.js` | 3.9 KB | JS | 扩展后台 Service Worker。三大核心职责：(1) 监听标签页加载完成，向新标签页注入脚本强制聚焦搜索框（带 4 次重试 + autoFocusSearch 设置检查）；(2) 响应 Ctrl+B 快捷键，以 `chrome.windows.create` 打开 400x580 的 popup 独立窗口；(3) 安装时注入 `declarativeNetRequest` 动态规则，剥离 `X-Frame-Options` 和 `CSP` 响应头以突破 iframe 嵌套限制；(4) 接收前台消息进行书签 URL 可达性扫描（10 秒超时熔断 + AbortController）。 |
| `content.js` | 480 B | JS | Content Script。在网页中拦截所有 `<a target="_blank">` 点击事件（捕获阶段优先于网站自身 JS），将 `target` 强制改为 `_self`，防止网站通过 `_blank` 弹出新标签页绕过新标签页扩展。 |
| `convert_gbk.ps1` | 213 B | PS1 | PowerShell 编码转换脚本。使用 `System.Text.Encoding.GetEncoding(936)`（GBK）读取 `script.js` 的原始字节，再以 UTF-8 编码写入 `script_utf8_gbk.js`，用于解决 JS 文件 GBK 乱码问题。 |
| `favicon.ico` | 3.0 KB | ICO | 扩展图标文件。在 manifest.json 中同时作为 16/48/128 三种尺寸的扩展图标，也在 `index.html` 中作为页面 favicon。 |
| `fonts.css` | 2.7 KB | CSS | 字体声明文件。通过 `@font-face` 声明 16 套本地英文字体：Audiowide / Bebas Neue / Black Ops One / Chakra Petch / Michroma / Monoton / Nova Square / Orbitron / Press Start 2P / Righteous / Share Tech Mono / Silkscreen / Space Mono / Teko / VT323 / Wallpoet。所有字体引用 `fonts/` 子目录下的 .ttf 文件，供时钟模块和小组件使用。 |
| `index.html` | 266.9 KB | HTML | **主页面（新标签页入口）**。2955 行的单页应用入口，包含：(1) Canvas 层：`interactive-canvas`（6 套动态背景引擎）、`solar-canvas`（太阳系）、`weather-canvas`（天气粒子效果）；(2) 背景层：ambient-layer（5 个动态光斑 blob）、bg-layer（静态背景）、bg-video（视频背景）、bg-overlay；(3) 用户模态框：邮箱/昵称编辑、头像上传、配置导入导出、一键恢复出厂；(4) 侧边栏：头像、动态导航分类、添加分类按钮；(5) 搜索栏：搜索引擎切换、语音输入、二维码、翻译/音乐/时间/待办工具栏；(6) 磁贴网格：拖拽排序的快捷方式网格；(7) 垃圾桶区域；(8) 内嵌 popup 搜索面板（与 popup.html 功能镜像的 overlay 版本）；(9) 设置面板（背景/布局/搜索/小组件/API 配置等七大类设置）；(10) 通知/Toast 层。加载 `Sortable.js`、`fonts.css`、`style.css`。 |
| `interactive-bg.js` | 52.1 KB | JS | **交互式背景引擎**（965 行）。Canvas 2D 驱动的 6 套动态主题渲染：Earth（蓝色系地球 + 月球 + 城市夜景光照）、Solar（红色系太阳 + 等离子体 + 日冕粒子）、Saturn（金棕色土星环 + 粒子模拟）、Quantum（紫色系量子星核 + 波函数粒子 + 共生连接线）、Cyber（青粉紫色赛博神经网 + 射线 + 神经突触）、Manifold（多彩流形共振波）。支持 FPS 帧率限制、标签页后台自动休眠、性能优化模式、主题颜色实时切换同步 CSS 变量。暴露 `updateInteractiveVisibility()` 全局接口供 script.js 调用。 |
| `manifest.json` | 1.2 KB | 配置 | （同上） |
| `popup.css` | 22.9 KB | CSS | Popup 窗口专属样式表（863 行）。定义 520x560 亮色主题 popup 的全部样式：导航栏（5 标签页切换 + 操作按钮）、搜索区域（输入框 + 引擎切换下拉）、结果列表（日期折叠/展开动画、favicon + 标题 + URL + 标签）、键盘提示栏、设置面板（toggle 开关 + 颜色选择器 + 按钮）、删除确认弹窗、导入分类选择弹窗。使用 `--ext-accent` CSS 变量实现主题色动态切换。 |
| `popup.html` | 10.9 KB | HTML | **扩展工具栏弹窗页面**（182 行）。Ctrl+B 触发的独立搜索窗口，结构与 index.html 内嵌 overlay 版本镜像但更精简：(1) 5 个标签页（书签/历史/下载/综合/搜索，比 .bak 多了"搜索"标签）；(2) 搜索栏 + 搜索引擎切换下拉；(3) 结果列表 + 空状态提示；(4) 键盘提示栏；(5) 设置面板（记忆上次标签/默认标签/主题色/排序/日期折叠/二次确认/清除统计/清除历史）；(6) 删除确认弹窗；(7) 导入分类弹窗。加载 `popup.css` 和 `popup.js`。 |
| `popup.html.bak` | 12.6 KB | HTML | `popup.html` 的旧版备份（212 行）。与当前版本的主要区别：(1) 只有 4 个标签页（书签/历史/下载/综合），缺少"搜索"标签页；(2) 设置面板选项更少（无"记忆上次标签页"/"二次确认"/"清除统计"/"清除历史"/"导入"等功能）；(3) 底部键盘提示无 Esc 关闭键。是 popup.html 升级前的快照备份。 |
| `popup.js` | 57.7 KB | JS | **Popup 窗口业务逻辑**（1094 行）。完整复刻 overlay 弹窗的搜索/管理功能，IIFE 封装。核心模块：(1) 设置管理：`loadSettings/saveSettings/applySettings`，localStorage 键 `binixovo_ext_settings`，支持 8 项设置；(2) 书签加载：`flattenBookmarks` 递归遍历 Chrome 书签树，支持按访问次数排序；(3) 历史记录：`chrome.history.search` 获取最近历史；(4) 下载记录：`chrome.downloads.search` 获取下载列表；(5) 综合搜索：统一搜索书签+历史+下载；(6) 渲染引擎：日期折叠分组、虚拟滚动、键盘导航（↑↓←→）、Enter 打开 / Del 删除；(7) Favicon 获取；(8) 导入当前页面到书签分类；(9) Toast 通知。 |
| `script.js` | 1.7 MB | JS | **主业务逻辑脚本**（33274 行）。整个扩展的核心 JS 文件，DOMContentLoaded 事件驱动。主要功能模块：(1) 标签页替换策略（"狸猫换太子"突破 Chrome 新标签页焦点限制）；(2) XSS 防护（escapeHTML/mergeDisp）；(3) API 配置模块（动态读取用户自定义 API 端点，含 25+ 默认 API：每日诗词/一言/Bing 壁纸/天气/IP 定位/翻译/OCR/图标获取等）；(4) 状态管理（localStorage 持久化，键 `tab_pro_settings_v370`）；(5) 磁贴系统：增删改查、拖拽排序（Sortable.js）、Alt+拖拽缩放、分类管理、文件夹支持；(6) 背景系统：6 种交互背景 + 静态图片/视频/纯色/Bing 每日壁纸，模糊/亮度/对比度/饱和度/色相调节；(7) 搜索栏：多引擎切换（Google/Bing/Baidu/DuckDuckGo + 自定义）、搜索建议/联想、语音输入；(8) 时钟组件：12/24 小时、秒针、发光效果、多字体、透明度；(9) 内置小组件：天气/IP 地址/翻译/音乐播放器/待办事项/计时器/番茄钟/汇率换算/下班倒计时；(10) 设置面板：7 大分类（我的/背景/布局/搜索/小组件/API/关于），磁贴边框样式、光斑动态开关、鼠标跟随光效；(11) 书签/历史轮播；(12) 配置导入导出/一键恢复出厂设置。 |
| `script.js.bak` | 1.6 MB | JS | `script.js` 的历史备份（版本 A）。与当前主文件版本接近但稍小（1.6 MB vs 1.7 MB），说明缺少部分后期新增功能（如下班倒计时、汇率换算等）。作为版本回退的安全快照。 |
| `script.js.bak_before_fix` | 1.7 MB | JS | `script.js` 的修复前备份。文件大小与当前主文件相同（1.7 MB），是某次 bug 修复操作前保留的完整快照，用于修复失败时回滚。 |
| `script.js.bak2` | 1.6 MB | JS | `script.js` 的历史备份（版本 B）。略小于当前版本，是中期迭代的快照。 |
| `script.js.bak3` | 1.5 MB | JS | `script.js` 的历史备份（版本 C）。文件更小（1.5 MB），是较早期版本，缺少更多后期功能。 |
| `script.js.bak4` | 1.6 MB | JS | `script.js` 的历史备份（版本 D）。与 bak 版本大小接近，是另一中间迭代快照。 |
| `script_utf8.js` | 1.7 MB | JS | `script.js` 的 UTF-8 编码变体。与当前主文件大小相同（1.7 MB），内容为 UTF-8 编码，可能是编码修复过程中的一个版本，确保中文字符正确显示。 |
| `script_utf8_fixed.js` | 1.6 MB | JS | `script.js` 的 UTF-8 编码修复版。略小于主文件（1.6 MB），是在 UTF-8 基础上进一步修复的版本。 |
| `script_utf8_gbk.js` | 1.6 MB | JS | 由 `convert_gbk.ps1` 生成的 UTF-8 编码文件。原始 `script.js` 若以 GBK 编码保存，此脚本将其正确读取后以 UTF-8 重新输出，确保跨平台兼容。 |
| `Sortable.js` | 133.4 KB | JS | **第三方拖拽排序库**（3388 行），SortableJS v1.15.6，MIT 许可。提供磁贴网格的拖拽排序、动画过渡、分组支持。在 `index.html` 中于 script.js 之前加载，作为全局 `Sortable` 暴露。script.js 中使用其 API 实现磁贴拖拽重排、分类间移动、拖入垃圾桶删除、Alt 键按下时动态禁用排序（防止与缩放冲突）。 |
| `style.css` | 259.5 KB | CSS | **主样式表**（8004 行）。定义新标签页全部视觉风格。核心模块：(1) CSS 变量体系（`--bg-base` / `--blob-1~5` / `--text-a/b` / 磁贴/搜索/光标光效参数等，支持主题色动态切换）；(2) 背景与动效层（`.ambient-layer` / `.blob` / `.bg-layer` / `.bg-overlay`）；(3) 布局（侧边栏 `.sidebar` / 网格 `.widget-grid` / 设置面板 `.settings-panel`）；(4) 磁贴样式（hover/active/拖拽/resizing 各状态，极小模式 `.widget-tiny`）；(5) 搜索栏（`.search-container` / 联想弹窗 `.suggestion-dropdown` / 引擎下拉）；(6) 模态框（用户信息编辑）；(7) 内置 popup overlay（`.ext-popup-container` 暗色主题版本，与 popup.css 亮色版本互补）；(8) 各小组件样式（天气 `.weather-widget` / 音乐 `.music-player` / 计时器 `.widget-timer` / 番茄钟 `.widget-pomodoro` / 下班倒计时 `.ow-*`）；(9) 响应式适配（zen-mode / 极小磁贴 `@media` / `widget-tiny` 类）；(10) 动画（`@keyframes auroraDrift` / blob float / 淡入淡出 / 垃圾桶呼吸）。 |

---

## 二、核心架构关系图

```
manifest.json  (扩展清单 - 入口注册)
    │
    ├─ newtab → index.html  (新标签页主页面)
    │               ├── Sortable.js      [拖拽排序库]
    │               ├── fonts.css        [字体声明]
    │               ├── style.css        [主样式]
    │               ├── interactive-bg.js [Canvas 动态背景引擎]
    │               └── script.js        [核心业务逻辑]
    │
    ├─ action.default_popup → popup.html  (工具栏弹窗)
    │               ├── popup.css  [弹窗样式]
    │               └── popup.js   [弹窗逻辑]
    │
    ├─ background.service_worker → background.js  (后台服务)
    └─ content_scripts → content.js  (注入到网页)
```

---

## 三、备份/变体文件说明汇总

| 文件名 | 与主文件关系 |
|---|---|
| `script.js.bak` | script.js 历史备份（版本 A），1.6 MB，略小于当前版本 |
| `script.js.bak_before_fix` | 某次 bug 修复前完整备份，1.7 MB，与当前主文件同大小 |
| `script.js.bak2` | script.js 历史备份（版本 B），1.6 MB，中期迭代快照 |
| `script.js.bak3` | script.js 历史备份（版本 C），1.5 MB，早期版本，功能最少 |
| `script.js.bak4` | script.js 历史备份（版本 D），1.6 MB，另一中间版本 |
| `script_utf8.js` | UTF-8 编码变体，1.7 MB，内容与主文件基本一致 |
| `script_utf8_fixed.js` | UTF-8 编码修复版，1.6 MB，在 UTF-8 基础上进一步修复 |
| `script_utf8_gbk.js` | 由 `convert_gbk.ps1` 从 GBK 源文件转 UTF-8 生成 |
| `popup.html.bak` | popup.html 旧版备份，缺少"搜索"标签页和多项设置功能 |

> **建议**：`.bak` 文件和 `script_utf8*.js` 变体文件共 9 个，合计占用约 12.5 MB。这些是开发过程中的版本快照和编码转换产物，实际运行时仅使用 `script.js` 和 `popup.html` 主文件，备份文件可以清理以减小项目体积。
